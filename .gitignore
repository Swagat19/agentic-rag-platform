# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/
# Virtual environments
.venv
.env

# Local scratch dirs for running ingestion against a subset of documents.
documents_smoke/

# Eval framework run outputs (timestamped, per-run); the framework code
# itself is tracked, but every run's reports live here and are not.
eval/results/*
!eval/results/.gitkeep

# Scratch artefacts from the gold-label bootstrap tools.
eval/_gold_id_proposals.yaml
eval/_bootstrap_candidates.yaml

# OS / editor noise
.DS_Store